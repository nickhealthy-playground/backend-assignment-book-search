external:
  naver:
    url: ${NAVER_API_URL:http://localhost:9090}
    headers:
      client-id: ${NAVER_API_CLIENT_ID:mock-client-id}
      client-secret: ${NAVER_API_CLIENT_SECRET:mock-client-secret}